<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.projectA.miniproject.repository.UserMapper">
    <insert id="save" useGeneratedKeys="true" keyProperty="user_id">
        insert into user_tb (user_id, username, password, name, email)
        values (default , #{username} , #{password} ,#{name}, #{email} )
    </insert>
</mapper>